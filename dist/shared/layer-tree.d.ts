import type { ComponentDefinition, ExemplaraDocument, RegionName } from '../core/types.js';
export type LayerTreeKind = 'document' | 'page' | 'region' | 'component' | 'slot';
export interface LayerTreeNode {
    id: string;
    kind: LayerTreeKind;
    label: string;
    children: LayerTreeNode[];
    pageIndex?: number;
    regionName?: RegionName;
    nodeId?: string;
    componentType?: string;
    slotName?: string;
    bindingCount?: number;
    hidden?: boolean;
    locked?: boolean;
    continuation?: boolean;
    autoGenerated?: boolean;
}
export type LayerDefinitionResolver = (componentType: string) => ComponentDefinition | undefined;
/** Build the complete authoring hierarchy used by outline/layer panels. */
export declare function buildDocumentLayerTree(doc: ExemplaraDocument, definitionFor?: LayerDefinitionResolver): LayerTreeNode;
export declare function countLayerTreeNodes(node: LayerTreeNode, kind?: LayerTreeKind): number;
/** Return the outline path from the document root to a component node. */
export declare function findLayerTreePath(root: LayerTreeNode, nodeId: string): LayerTreeNode[] | null;
