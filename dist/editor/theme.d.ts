import type { EditorThemeTokens } from './extension-types.js';
/** Render the typed host theme seam as scoped CSS custom properties. */
export declare function renderEditorThemeStyle(tokens?: EditorThemeTokens): string | undefined;
